import React from 'react'

export default function Testimonials() {
  // Testimonials removed per request (previously contained sample/test data)
  // Keep the component to preserve layout wiring, but render nothing.
  return null
}
